#include <stdio.h>
#include <stdbool.h>

#include "FastLinkedList.h"

int main(void)
{
    printf("Fast Linked List Implementation\n");
    FastLinkedList *list = NULL; // TODO: malloc and zerois

    for (int i = 0; i < 10; ++i)
    {
        printf("Hello: %d\n", i);
    }
    bool isUpper = true;
    return 0;
}
